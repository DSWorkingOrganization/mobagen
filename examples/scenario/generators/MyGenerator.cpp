#include "MyGenerator.h"

using namespace std;

std::vector<Color32> Generate(int sideSize, float displacement = 0) {
  std::vector<Color32> colors;

  //auto limiarized Limiar(colors, side)
}

std::string GetName() { return "PEAKS"; }